// Given a non-empty array of integers, return the result of multiplying the values together in order.

const multipliedValues = arrayInt => {

}

console.log(multipliedValues([1, 2, 3, 4])); //
console.log(multipliedValues([1, 3, 3, 4])); //
console.log(multipliedValues([1, 2, 0, 44])); //
console.log(multipliedValues([3, 4])); //